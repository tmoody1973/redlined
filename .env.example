# Convex deployment URL (populated by `npx convex dev`)
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=

# API keys are stored in Convex environment config, NOT in .env files.
# After running `npx convex dev`, set keys via:
#   npx convex env set ANTHROPIC_API_KEY <your-anthropic-api-key>
#   npx convex env set CENSUS_API_KEY <your-census-api-key>
#
# Get API keys:
#   Anthropic: https://console.anthropic.com/
#   Census: https://api.census.gov/data/key_signup.html
